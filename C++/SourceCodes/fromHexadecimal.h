#pragma once

#include <string>
#include <sstream>
#include <bitset>
#include <cstring>

extern "C" __declspec(dllexport) const char* hexadecimalToBinary(const char*);
extern "C" __declspec(dllexport) const char* hexadecimalToOctal(const char*);
extern "C" __declspec(dllexport) const char* hexadecimalToDecimal(const char*);
